{
  "validation": {
    "commands": [
      "pnpm format"
    ],
    "timeout": 600,
    "forbiddenArtifactPaths": [
      "certification/",
      "baseline/",
      "config/render-profiles/"
    ]
  }
}
