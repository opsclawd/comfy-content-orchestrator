{
  "validation": {
    "additionalCommands": [
      "pnpm check:hooks",
      "pnpm format",
      "pnpm test:db",
      "pnpm test:assembly",
      "pnpm test:kokoro",
      "pnpm install --frozen-lockfile",
      "pnpm check:control-plane"
    ],
    "timeout": 900,
    "forbiddenArtifactPaths": ["certification/", "baseline/", "config/render-profiles/"]
  }
}
